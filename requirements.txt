# mip / mipcore — dependências diretas (faixas do pyproject.toml)
# Python >= 3.11. Instalar o pacote editável: pip install -e mip/MIP
numpy>=2.0
pandas>=2.2
scipy>=1.12
xlrd>=2.0
openpyxl>=3.1
pytest>=7.0   # suíte de testes
